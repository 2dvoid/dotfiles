return {
  -- Source
  "echasnovski/mini.pairs",

  -- Triggering event
  event = "InsertEnter",

  -- Options / Settings
  opts = {} 

  -- Power Switch
  -- [Won't be needed here. If "opts" is provided, this Power Switch is never needed. It is automatically called by the Lazy package manager.]
  --
  -- config = function()
  --  require("mini.pairs").setup({})
  -- end
}

